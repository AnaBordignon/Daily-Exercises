#if !defined(BOB_H)
#define BOB_H

#include <string>

namespace bob {

	bool not_alpha(char c);

	bool is_uppercase_string(std::string input);

	std::string hey(std::string input);
}  // namespace bob

#endif // BOB_H